services:
  app:
    build: .
    container_name: saq-sommelier
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:8000:8000"
    networks:
      - internal

networks:
  internal:
    external: true
