#!/usr/bin/env bash
# Pre-push hook — fast safety net (pre-commit already ran full coverage).
# Install with: make install

set -e

echo "🔍 Running pre-push checks..."

echo ""
echo "📦 Checking lock files..."
cd backend && poetry lock --check && cd ..
cd scraper && poetry lock --check && cd ..
cd core && poetry lock --check && cd ..

echo ""
make lint
echo ""
make test

echo ""
echo "✅ All checks passed — pushing."
