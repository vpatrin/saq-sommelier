#!/usr/bin/env bash
# Pre-commit hook — fast lint only (coverage runs in pre-push).
# Install with: make install

set -e

echo "🔍 Running pre-commit checks..."

echo ""
make lint

echo ""
echo "✅ Lint passed — committing."
